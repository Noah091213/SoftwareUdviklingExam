#ifndef HERO_HPP
#define HERO_HPP
#include <string>

class Hero {

    public:

    private:
        
        std::string  heroName;
        int currentHP;
        int currentXP;
        int currentLevel;
        int strength;
        
};

#endif